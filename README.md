Exécution du code :
Pour faire tourner le code depuis le début:
- Il faut exécuter 1_computation puis 2_models pour obtenir les résultats principaux du test. 
- Il faut exécuter 5_comput_uk puis 6_models_uk pour obtenir les résultats sur le UK dataset.
- Tous les Excels sont déjà push, donc faire tourner directement 2_models et 7_models_uk fonctionnent

Environnement nécessaire : 
- Python 3.12 (si version trop récente problème de compatibilité avec py torch > nécessaire uniquement pour lancer test_nn2)
- numpy, pandas, openpyxl, scikit-learn, xgboost, statsmodels, scipy, matplotlib, tqdm
- torch et skorch (uniquement pour test_nn2.ipynb, qui ne converge pas)